---
- name: install package
  apt: name={{ item }}
  with_items:
    - nginx

- name: create nginx configuration file
  template:
    src=nginx.conf.j2
    dest=/etc/nginx/sites-available/{{ site_name }}
