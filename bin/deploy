#!/bin/bash -x

# http://stackoverflow.com/questions/59895/can-a-bash-script-tell-what-directory-its-stored-in
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
export GIT_SSH=$(readlink -f ${DIR}'/../bin/connect')
exec git push $@
#GIT_SSH=miao echo $GIT_SSH
